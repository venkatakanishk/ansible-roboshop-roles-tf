# MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws.ssm_parameter', '/roboshop/{{ env }}/mysql_root_password', region='us-east-1', with_decryption=True) }}"
MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/{{ environment }}/mysql_root_password', region='us-east-1', decrypt=True) }}"